"""App config for email_validation module."""
from django.apps import AppConfig


class EmailValidationConfig(AppConfig):
    """App config for email_validation module."""

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'email_validation'
